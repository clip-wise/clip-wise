import React from 'react';
import ReactDOM from 'react-dom/client';
import SidePanelContent from './index';

ReactDOM.createRoot(document.getElementById('root') as HTMLElement).render(
  <React.StrictMode>
    <SidePanelContent />
  </React.StrictMode>
);
